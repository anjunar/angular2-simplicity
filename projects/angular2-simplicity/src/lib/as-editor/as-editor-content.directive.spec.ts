import { AsEditorContentDirective } from './as-editor-content.directive';

describe('AsEditorContentDirective', () => {
  it('should create an instance', () => {
    // const directive = new AsEditorContentDirective();
    // expect(directive).toBeTruthy();
  });
});
