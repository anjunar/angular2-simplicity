import { AsErrorDirective } from './as-error.directive';

describe('AsErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new AsErrorDirective();
    expect(directive).toBeTruthy();
  });
});
