import { AsInputDirective } from './as-input.directive';

describe('AsInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AsInputDirective();
    expect(directive).toBeTruthy();
  });
});
